spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/backend?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: Nwafu101617
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    
server:
  port: 8080
