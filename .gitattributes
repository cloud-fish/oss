# 统一换行符规则：文本文件入库时统一为 LF

# 源代码、配置、文档：统一 LF
* text=auto eol=lf

# Windows 专用脚本必须保持 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
mvnw text eol=lf

# 二进制文件不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.jar binary
